const verionHeader = require('./header-validator');
const validateVersion = require('./validate-version');
const modifyHeader = require('./modify-header');

module.exports = {
  verionHeader,
  validateVersion,
  modifyHeader,
};
